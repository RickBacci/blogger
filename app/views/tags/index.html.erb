<h1>All Tags</h1>
<ul>
  <% @tags.each do |tag| %>
    <p><li><%= tag.name %></li></p>  
    <p><%= link_to "Delete", tag_path(tag), method: :delete %></p>
  <% end %>
</ul>

<%= link_to "Home", articles_path %>
